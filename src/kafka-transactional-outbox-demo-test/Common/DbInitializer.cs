public static class DbInitializer
{
    public static void PopulateDefaultRecords(BooksDbContext context)
    {
        // var book = new Book(
        //     Guid.NewGuid(),
        //     "Book #" + 1,
        //     "Author " + 1,
        //     DateTime.UtcNow);

        // context.Books.Add(book);
        // context.SaveChanges();
    }
}